import { Typography } from "@mui/material";

const SectionTitle = ({ children }: any) => (
  <Typography variant="h6" color="#000" align="left" sx={{ marginBottom: "16px" }}>{children}</Typography>
);

export default SectionTitle;